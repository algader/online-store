# تعليمات الأمان - تعيين المديرين

## ⚠️ مهم جداً: كيفية إنشاء حساب مدير

### ❌ الطريقة الخاطئة (ثغرة أمنية)
لا يمكن للمستخدم تعيين نفسه كمدير عبر صفحة التسجيل. تم إغلاق هذه الثغرة الأمنية.

### ✅ الطريقة الصحيحة (آمنة)

#### الخطوة 1: إنشاء حساب عادي
1. افتح الموقع على http://localhost:3000
2. انتقل إلى صفحة التسجيل
3. أنشئ حساب جديد (سيكون الدور user تلقائياً)

#### الخطوة 2: ترقية الحساب إلى مدير
افتح Terminal وقم بتشغيل الأمر التالي:

```bash
cd backend
npm run make-admin
```

سيعرض لك قائمة بجميع المستخدمين:
```
📋 المستخدمون الحاليون:

1. محمد أحمد (mohammad@test.com) - user
2. أحمد علي (ahmed@test.com) - user

📧 أدخل البريد الإلكتروني للمستخدم الذي تريد جعله مديراً:
```

أدخل البريد الإلكتروني وسيتم ترقيته إلى مدير.

#### الخطوة 3: تسجيل الدخول كمدير
الآن يمكنك تسجيل الدخول بالحساب المُرقّى والوصول إلى لوحة التحكم.

---

## 🔐 ملاحظات أمنية

### ما تم إصلاحه:
- ✅ إزالة إمكانية تحديد `role` من طلب التسجيل
- ✅ الرتبة الافتراضية دائماً `user`
- ✅ تعيين المدير يتم فقط عبر سكريبت مخصص يتطلب وصول مباشر للخادم
- ✅ حماية من هجمات Privilege Escalation

### التحذيرات:
- ⚠️ لا تشارك البريد الإلكتروني وكلمة المرور للمدير مع أي شخص
- ⚠️ استخدم كلمة مرور قوية للحساب الإداري
- ⚠️ الوصول إلى سكريبت make-admin يتطلب وصول للخادم (SSH أو محلي)
- ⚠️ في بيئة الإنتاج، احمِ هذا السكريبت ولا تجعله متاحاً عبر الإنترنت

---

## 🛠️ استخدام سكريبت التهيئة الأولية

إذا كنت تريد إنشاء بيانات تجريبية مع مدير جاهز:

```bash
cd backend
npm run seed
```

سيتم إنشاء:
- حساب مدير: `admin@store.com` / `admin123`
- حساب مستخدم عادي: `user@test.com` / `user123`
- منتجات تجريبية

**تحذير**: غيّر كلمة المرور فوراً بعد أول تسجيل دخول!
